---
import Layout from '../layouts/Layout.astro';
import CustomSearch from '../components/CustomSearch';

const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<Layout
  title="Search - Daily Scraps"
  description="Search blog posts"
>
  <main>
    <section>
      <h1>🔍 記事を検索</h1>
      <p>キーワードを入力して記事を検索できます</p>
      <CustomSearch baseUrl={baseUrl} client:load />
    </section>
  </main>
</Layout>

<style>
  main {
    width: calc(100% - 2em);
    max-width: 100%;
    margin: 0;
  }

  section {
    max-width: 720px;
    margin: 2rem auto;
    padding: 0 1rem;
  }

  h1 {
    font-size: 2.5rem;
    font-weight: 700;
    line-height: 1;
    margin-bottom: 1rem;
    background: linear-gradient(
      135deg,
      var(--accent) 0%,
      var(--accent-dark) 100%
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  p {
    color: rgb(var(--gray));
    margin-bottom: 2rem;
  }

  @media (max-width: 768px) {
    section {
      margin: 1.5rem auto;
    }

    h1 {
      font-size: 2rem;
    }

    p {
      font-size: 0.95rem;
      margin-bottom: 1.5rem;
    }
  }

  @media (max-width: 480px) {
    main {
      width: calc(100% - 1.5em);
    }

    section {
      margin: 1rem auto;
      padding: 0 0.75rem;
    }

    h1 {
      font-size: 1.75rem;
      margin-bottom: 0.75rem;
    }

    p {
      font-size: 0.9rem;
      margin-bottom: 1rem;
    }
  }
</style>
